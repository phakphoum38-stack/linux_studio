import 'package:flutter/material.dart';

class PackageManagerScreen extends StatelessWidget {
  const PackageManagerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Package Manager Module'),
    );
  }
}
